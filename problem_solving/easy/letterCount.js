// Write a function called letterCount() that takes a word and returns the
// number of letters in that word. For example, letterCount('abc') should
// return 3.

var letterCount = function(string) {
	return string.length;
};
